server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 10.211.55.6:8848         #配置 nacos server 的地址

management:
  endpoints:
    web:
      exposure:
        include: '*'